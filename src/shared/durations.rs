/// MCP mutation highlight fade duration in milliseconds.
pub const HIGHLIGHT_FADE_MS: u64 = 2000;

/// Undo coalescing window in milliseconds — consecutive keystrokes within
/// this window are grouped into a single undo entry.
pub const UNDO_COALESCE_MS: u64 = 300;
