// Rich text editor — main Slint entry point.
// Imports theme and tokens from RustUI (slint-ui-templates).

export component AppWindow inherits Window {
    title: "Rich Text Editor";
    width: 900px;
    height: 700px;

    // TODO: integrate slint-ui-templates desktop shell
    // TODO: add EditorCanvas, toolbar, statusbar
}
