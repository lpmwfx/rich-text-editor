// Rich text editor — main entry point.
// Imports desktop shell from slint-ui-templates (RustUI).

import { AppWindow } from "desktop/app-window.slint";
import { Colors, Spacing, Type, Settings } from "tokens/theme.slint";
import { FluentIcons } from "tokens/fluent/mod.slint";

export { AppWindow, Colors, Settings }
